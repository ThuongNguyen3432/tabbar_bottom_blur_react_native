import type { ThemeSpacing } from './theme.types';

/** A 4pt scale. Every gap in the app should come from here. */
export const spacing: ThemeSpacing = {
  xs: 4,
  sm: 8,
  md: 16,
  lg: 24,
  xl: 32,
  xxl: 48,
};
